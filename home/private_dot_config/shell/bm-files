# These files automatically update when edited/saved in vim:

# keys	filename				description
cfshf	${XDG_CONFIG_HOME:-$HOME/.config}/shell/bm-files	# This file, a list of bookmarked files
cfshd	${XDG_CONFIG_HOME:-$HOME/.config}/shell/bm-dirs		# A list of bookmarked directories similar to this file
cfxc	${XDG_CONFIG_HOME:-$HOME/.config}/x11/xresources	# Colors, themes and variables for X11
lsdc	~/.local/src/dwmblocks/config.h				# dwmblocks: the status bar for dwm


# These do not update automatically, but on the next new instance of a program:

cfvc	${XDG_CONFIG_HOME:-$HOME/.config}/nvim/init.lua		# vim/neovim config
cfzc	$ZDOTDIR/.zshrc						# zsh (shell) config
cfsha	${XDG_CONFIG_HOME:-$HOME/.config}/shell/aliasrc		# aliases used by zsh (and potentially other shells)
cfshp	${XDG_CONFIG_HOME:-$HOME/.config}/shell/profile		# profile file for login settings for zsh
cfmtc	${XDG_CONFIG_HOME:-$HOME/.config}/mutt/muttrc		# mutt (email client) config
cfnc	${XDG_CONFIG_HOME:-$HOME/.config}/newsboat/config	# newsboat (RSS reader)
cfnu	${XDG_CONFIG_HOME:-$HOME/.config}/newsboat/urls		# RSS urls for newsboat
cfncb	${XDG_CONFIG_HOME:-$HOME/.config}/ncmpcpp/bindings	# ncmpcpp (music player) keybinds file
cfncc	${XDG_CONFIG_HOME:-$HOME/.config}/ncmpcpp/config	# ncmpcpp (music player) config
cflc	${XDG_CONFIG_HOME:-$HOME/.config}/lf/lfrc		# lf (file browser) config
cfls	${XDG_CONFIG_HOME:-$HOME/.config}/lf/scope		# lf's scope/preview file
cfsxk	${XDG_CONFIG_HOME:-$HOME/.config}/sxiv/exec/key-handler	# sxiv (image viewer) key/script handler
